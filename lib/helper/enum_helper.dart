enum ToastStatus { success, failure, warning }

enum ProcessState { none, loading, success, failure }

enum Method { post, get, put, patch, delete }

enum BodyType { json, urlencoded }

enum DashboardData {
  directSale,
  initialSubscription,
  recurringSubscription,
  subSalvage
}
